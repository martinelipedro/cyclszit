#include <SDL2/SDL.h>
#include "vector2.hpp"

Vec2<int> get_absolute_position(int x, int y);
Vec2<int> get_relative_position(int x, int y);
uint32_t color_from_rgb(uint8_t r, uint8_t g, uint8_t b, uint8_t a);
